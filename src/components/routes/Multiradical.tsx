import React from "react";
import styled from "styled-components"

interface Props {}
  
const div = styled.div``;

const Multiradical: React.FC<Props> = ({}) => { 
  return (
    <div>MULTIRADICALS</div>
  );
}

export default Multiradical;
